{
  "buildCommand": "cd frontend/voucher && npm install && npm run build",
  "outputDirectory": "frontend/voucher/dist",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
